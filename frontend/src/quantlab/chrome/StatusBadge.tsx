// The implementation moved to the shared primitives; this re-export keeps the
// surface's existing import paths stable.
export { StatusBadge, type StatusTone } from '../../components/ui/status-badge';
